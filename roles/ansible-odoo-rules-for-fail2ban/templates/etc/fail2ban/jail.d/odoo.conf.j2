[odoo]
enabled = true
port = http,https
bantime = 900  # ; 15 min ban
maxretry = 15  # Pattern will be found 5 times for a successful login
findtime = 1800  # ; within 1 min  /!\ Should be adjusted with the TZ offset (1hrs for Odoo, as it logs in UTC time but fail2ban in local)
#logpath = /var/log/odoo.log  ;  set the actual odoo log path here
logpath = /var/log/apache2/access.log
